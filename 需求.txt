1、pull上传数据，建立长连接，并拉取消息。
2、有一个营销平台，下发任务。
3、需要下发的任务缓存在redis中。
4、静态上报的数据存在mongo和redis中。如果redis中没找到就去mongo中找，设备信息。
5、




1、任务发送平台（创建任务，下发任务等）。
    任务保存在pgsql库中，定时（5s）从库中查找广告，并推送出去（放在redis有序队列中），等待pull请求。
    pull请求有个指针（acks），用于表示目前请求到哪个广告了。

    id：xxx
    手机类型：iphone，小咪，华为...
    手机imie：xxxxx

    前端使用弹框来表示信息的接受与展示。并返回展示信息。
    后期反馈点击信息。

2、任务定量功能。任务发送量存在redis中。
3、反馈信息：
4、静态数据上报放在mongo中，并缓存到redis中。
5、广告匹配模块，用于匹配广告
    匹配手机型号或者imie号。
6、添加权限模块（jwt授权）
7、源代码中比较好的思想


